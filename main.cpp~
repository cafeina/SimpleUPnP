#include <string>
#include <iostream>
#include <unistd.h>

#include "basic_upnp.h"

using std::string;

void get_in_ip()
{

}

int main()
{
    SimpleUPnP u;
    int port = 25454;
    //u.GetStatusInfo();
    //u.GetNatRSIPStatus();
    std::cout << u.AddPortMapping(80, "TCP", port, u.InternalIP()["IPV4"][0], "Gleison ROCKS", 0) << std::endl;
    std::cout << u.GetExternalIPAddress() << std::endl;
    //u.GetConnectionTypeInfo();
    sleep(120);

    u.DeletePortMapping(80, "TCP");

    return 0;

    //string external_ip = u.exec_command("GetExternalIPAddress");
    
    //u.add_port_mapping(5456, 5456, "UDP", "192.168.1.101");
    //sleep(10);
    //u.exec_command("DeletePortMapping", "TCP", port);

    

}
